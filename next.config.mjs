/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  swcMinify: true,
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
}

export default nextConfig
